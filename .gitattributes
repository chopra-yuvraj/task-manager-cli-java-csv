# Normalize all text files to LF in repo
* text=auto

# Enforce LF for Markdown and code
*.md   text eol=lf
*.java text eol=lf

# Keep Windows endings for Windows-only scripts (if you add them later)
*.bat  text eol=crlf
*.ps1  text eol=crlf
